import Publication from '@/components/(pages)/publication/Publication';
import React from 'react';

const page = () => {
    return (
        <div>
            <Publication/>
        </div>
    );
};

export default page;