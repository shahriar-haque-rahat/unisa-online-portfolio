import Conference from '@/components/(pages)/conference/Conference';
import React from 'react';

const page = () => {
    return (
        <div>
            <Conference/>
        </div>
    );
};

export default page;